<p id="notice"><%= notice %></p>

<p>
  <h2><%= @brewery.name %></h2>
</p>

<p>
  <i>Established in
  <%= @brewery.year %> </i>
  <% if not @brewery.site_link.nil? %>
    You can find this brewery's website <%= link_to 'here', @brewery.site_link %>
  <% end %>
  <% if not @brewery.active  %>
    <span class="label label-info">retired</span>
  <% end %>
</p>

<p>
  <strong>Number of beers:</strong>
  <%= @brewery.beers.count %>
</p>

<p>
 <% @brewery.beers.each do |beer| %>
   <%= link_to beer.name, beer %>
 <% end %>
</p>

<p>
<% if @brewery.ratings.empty? %>
         This brewery's beers have not yet been rated!
<% else %>
	Has <%= pluralize @brewery.ratings.count, 'rating', 'ratings' %>,
	average <%= @brewery.average_rating %>
<% end %>
</p>

<%= button_to "Delete", @brewery, :class => "btn btn-danger btn-xs", :method => :delete, data: { confirm: 'Are you sure?' } %>
<%= button_to "Edit", edit_brewery_path(@brewery), :class => "btn btn-warning btn-xs", :method => :get %>
<br>
<%= link_to 'Back', breweries_path %>
